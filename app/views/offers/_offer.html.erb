<li>
  <span class="content"><%= offer.content %>


<% if current_user?(offer.user) %>
    <%= link_to "delete", offer, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: offer.content %>
  <% end %>
  </span>
  </li>