<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Create a post for something you need..." %>
  </div>
  <%= f.submit "Share", class: "btn btn-large btn-primary" %>
<% end %>